# Amber Lang IR Probe Walkthrough

This walk-through keeps the domain vocabulary close to the data instead of burying it in prose.

| Case | Focus | Score | Lane |
| --- | --- | ---: | --- |
| baseline | IR pressure | 143 | ship |
| stress | lowering drift | 183 | ship |
| edge | stack depth | 183 | ship |
| recovery | diagnostic reach | 136 | watch |
| stale | IR pressure | 214 | ship |

Start with `stale` and `recovery`. They create the widest contrast in this repository's fixture set, which makes them better review anchors than the middle cases.

The useful comparison is `IR pressure` against `diagnostic reach`, not the raw score alone.
